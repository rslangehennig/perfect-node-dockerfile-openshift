apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfect-node.fullname" . }}-test-connection"
  labels:
    {{- include "perfect-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfect-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
